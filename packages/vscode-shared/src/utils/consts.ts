// Private defintions for utils folder

export const AIF_PROTOCOL = "aif";
export const AIF_URI_PREFIX = `${AIF_PROTOCOL}://`;
export const AIF_AGENTS_PREFIX = `${AIF_URI_PREFIX}agents/`;

export enum LmProviderPropValueType {
    Plain = "plain",
    Secret = "secret",
}
